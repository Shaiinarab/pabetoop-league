# راهنمای مدیر مسابقات — لیگ فوتبال نوجوانان

> این راهنما برای مدیر مسابقات (کاربر اصلی سامانه) نوشته شده است.
> ورود: `http://127.0.0.1:8080/admin` با نام کاربری `admin` و گذرواژه‌ای که
> هنگام اجرا تعیین شده است. همه صفحات فارسی و راست‌به‌چپ هستند.

## ۱. ورود و خروج

- در صفحه ورود، نام کاربری و گذرواژه را وارد کنید. پیام «نام کاربری یا
  گذرواژه نادرست است.» یعنی یکی از این دو اشتباه است (به‌عمد مشخص نمی‌شود
  کدام).
- پس از ۵ بار تلاش ناموفق از یک نشانی، ورود برای ۱۵ دقیقه قفل می‌شود.
- اگر پیام «نشست شما منقضی شده است؛ دوباره وارد شوید.» دیدید، دوباره وارد
  شوید (نشست‌ها ۱۲ ساعته هستند).

## ۲. مسیر کاری پیشنهادی برای یک فصل

فصل ریشهٔ همهٔ داده‌هاست: هر مسابقه، ثبت‌نام و بازی به یک فصل وابسته است. پس
مسیر از فصل شروع می‌شود، نه از باشگاه.

۱. **فصل** (`مدیر → فصل‌ها`): یک فصل بسازید (مثلاً «۱۴۰۶–۱۴۰۷») و آن را
   **فعال** کنید. تا وقتی فصل فعالی وجود نداشته باشد، بخش «مسابقات» چیزی
   برای ساختن نشان نمی‌دهد و سایت عمومی داده‌ای نمایش نمی‌دهد. فقط **یک فصل**
   می‌تواند فعال باشد؛ با فعال‌کردن یک فصل، فصل‌های دیگر خودکار غیرفعال
   می‌شوند و فصل هرگز حذف نمی‌شود.
۲. **باشگاه‌ها** (`مدیر → باشگاه‌ها`): نام رسمی باشگاه را دقیق وارد کنید.
   نام رسمی مرجع نهایی است — سامانه هرگز نام را اصلاح یا ادغام نمی‌کند.
   «نمونه ب» و «نمونه ب نوین» دو باشگاه متفاوت‌اند.
۳. **تیم‌ها** (`مدیر → تیم‌ها`): هر تیم متعلق به دقیقاً یک باشگاه است
   (مثلاً «نمونه ب ۲») و در فرم ساخت تیم، **ردهٔ سنی** آن را هم انتخاب
   می‌کنید. رده‌های سنی (۱۰ تا ۱۴) هنگام راه‌اندازی سامانه ساخته می‌شوند؛
   فهرست را انتخاب کنید، نیازی به ساختن آن‌ها نیست. در هر لیگ برتر هر رده
   سنی، هر باشگاه فقط **یک تیم** می‌تواند داشته باشد (سامانه جلوی ثبت دوم را
   می‌گیرد).
   در هر ردیف فهرست، دکمهٔ **«ویرایش»** نام نمایشی، برچسب و ردهٔ سنی تیم را
   قابل اصلاح می‌کند — یک غلط تایپی را لازم نیست با حذف و ساخت دوباره جبران
   کنید. **باشگاه در حالت ویرایش قفل است**: جابه‌جا کردن تیم بین باشگاه‌ها
   یک ویرایش نیست (سوابق و نام تیم به باشگاه گره خورده‌اند)؛ اگر تیم زیر
   باشگاه اشتباهی ثبت شده، آن را «غیرفعال» و تیم تازه‌ای در باشگاه درست
   بسازید.
۴. **مسابقات و ثبت‌نام** (`مدیر → مسابقات`): مسابقه (رده سنی + سطح + گروه)
   را انتخاب کنید و تیم‌ها را ثبت‌نام کنید. تیمی که در مسابقه‌ای بازی دارد،
   قابل حذف از ثبت‌نام نیست.
۵. **برنامه بازی‌ها** (`مدیر → خانه`، دکمهٔ «مدیریت برنامهٔ بازی‌ها»):
   بازی‌ها را دستی وارد کنید (هفته، تاریخ شمسی، ساعت، زمین). بازی تکراری
   (همان مسابقه، همان دو تیم، همان هفته) پذیرفته نمی‌شود.
۶. **ثبت سریع نتایج** (`مدیر → ثبت نتیجه`): هفته را انتخاب کنید، دو عدد گل را
   وارد و Enter بزنید — تمرکز خودکار به بازی بعد می‌رود و جدول زنده
   به‌روز می‌شود. برای برگرداندن یک بازی به حالت برنامه‌ریزی‌شده، «پاک‌کردن
   نتیجه» را بزنید.
۷. **مشاهده جدول** (`مدیر → ثبت نتیجه`، بخش «جدول»): جدول همیشه از بازی‌های
   تمام‌شده محاسبه می‌شود (۳ امتیاز برد، ۱ مساوی؛ ترتیب: امتیاز → تفاضل →
   گل زده → حروف الفبا).

## ۳. قوانین مهم صحت داده

- هر دو تیم یک بازی باید در همان مسابقات ثبت‌نام شده باشند.
- بازی تمام‌شده حتماً هر دو گل را می‌خواهد؛ بازی برنامه‌ریزی‌شده هیچ گلی ندارد.
- گل‌ها عدد صحیح صفر یا بیشتر هستند.
- **فقط یک فصل در هر زمان فعال است.** با فعال‌کردن یک فصل، فصل‌های دیگر در
  همان عملیات غیرفعال می‌شوند و فقط دادهٔ فصل فعال در سایت عمومی دیده
  می‌شود.
- باشگاه/تیمی که سابقه بازی یا ثبت‌نام دارد، **حذف کامل نمی‌شود** — آن را
  غیرفعال کنید تا سوابق تاریخی سالم بماند. فصل‌ها هرگز حذف نمی‌شوند.

## ۴. پشتیبان و گزارش تغییرات

- **پشتیبان‌گیری** (`مدیر → پشتیبان‌گیری`): دکمه «دانلود پشتیبان» یک فایل سالم و
  کامل از پایگاه داده می‌دهد. نام فایل تاریخ‌دار است. این کار را مرتب انجام
  دهید و فایل را بیرون از سرور نگه دارید.
- **گزارش تغییرات** (`مدیر → گزارش تغییرات`): هر تغییری که در بخش مدیریت انجام
  می‌شود (چه کسی، چه چیزی، قبل و بعد) اینجاست. اگر چیزی اشتباه شد، اول
  اینجا ببینید چه اتفاقی افتاده است.

## ۵. خطاهای پرتکرار و معنی آن‌ها

| پیام | معنی | چه کنید |
|---|---|---|
| «باشگاهی با این نام قبلاً ثبت شده است» | نام تکراری | نام رسمی را بررسی کنید؛ اگر واقعاً باشگاه دیگری است، تفاوت را در نام لحاظ کنید |
| «یک باشگاه در هر لیگ برتر فقط یک تیم می‌تواند داشته باشد» | قانون تک‌تیمی لیگ برتر | تیم دوم آن باشگاه را در لیگ ۱ ثبت کنید |
| «هر دو تیم مسابقه باید در همین مسابقات ثبت‌نام شده باشند» | تیم ثبت‌نام نشده | اول هر دو تیم را در همان مسابقات ثبت‌نام کنید |
| خطای هفته/بازی تکراری | برنامه تکراری | هفته و ترکیب دو تیم را بررسی کنید |

## ۶. محدودیت‌های دانستنی

- **کل مسیر از رابط کاربری انجام می‌شود:** ساخت و فعال‌سازی فصل
  (`مدیر → فصل‌ها`)، باشگاه، تیم، مسابقه، ثبت‌نام، بازی و نتیجه. روی یک
  نصب تازه پایگاه داده خالی است و نخستین کار شما ساختن یک فصل است؛ فصل
  نمایشی «۱۴۰۵–۱۴۰۶» فقط وقتی هست که با `SEED=1` اجرا کرده باشید
  (جزئیات در `DEPLOY.md` §۴).
- **ردهٔ سنی تیم هم ذخیره و هم نمایش داده می‌شود.** هنگام ساخت تیم انتخاب
  می‌شود و در ستون «ردهٔ سنی» فهرست `مدیر → تیم‌ها` دیده می‌شود. برای تیم‌هایی
  که پیش از مهاجرت `0004` ساخته شده‌اند (یا با `cmd/seed` ساخته می‌شوند) رده‌ای
  در پایگاه داده ثبت نشده و به‌جای مقدار، «—» نشان داده می‌شود؛ ردهٔ آن‌ها
  قابل استنتاج نبود و از روی حدس پر نشده است (`DECISIONS.md` D20). برای
  همین تیم‌ها اکنون می‌توانید از «ویرایش» ردهٔ درست را خودتان انتخاب کنید —
  سامانه هیچ‌گاه رده‌ای را حدس نمی‌زند.
- **بارگذاری گروهی از فایل (CSV) هنوز به دکمهٔ نهایی وصل نشده است** — تا آن
  زمان بازی‌ها را دستی وارد کنید و هر ابهامی را گزارش دهید.
