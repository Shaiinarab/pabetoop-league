{
  "name": "pabetoop-league",
  "version": "0.1.0",
  "private": true,
  "description": "Youth competition platform: fixtures, results, standings — white-label, single Go binary, SQLite",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "bun build web/static/js/app.ts --outdir web/static/js --minify",
    "test": "bun test"
  },
  "dependencies": {
    "htmx.org": "4.0.0",
    "typescript": "7.0.2"
  }
}
